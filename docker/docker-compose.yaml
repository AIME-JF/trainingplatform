services:
  postgres:
    image: ccr.ccs.tencentyun.com/z5882852/postgres:15-alpine
    container_name: police-training-postgres
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      PGDATA: /var/lib/postgresql/data/pgdata
      TZ: Asia/Shanghai
    volumes:
      - ./volumes/postgres/data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20

  redis:
    image: ccr.ccs.tencentyun.com/z5882852/redis
    container_name: police-training-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - ./volumes/redis/data-v2:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  minio:
    image: quay.io/minio/minio:RELEASE.2025-04-22T22-12-26Z
    container_name: police-training-minio
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY}
    command: server /data --console-address ":9001"
    volumes:
      - ./volumes/minio/data:/data

  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    image: police_training_backend:latest
    container_name: police-training-backend
    restart: unless-stopped
    env_file:
      - ./.env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_started
    volumes:
      - ./volumes/backend/logs:/app/logs
    command: api

  worker:
    build:
      context: ../backend
      dockerfile: Dockerfile
    image: police_training_backend:latest
    container_name: police-training-worker
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      CELERY_CONCURRENCY: ${CELERY_CONCURRENCY:-10}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      backend:
        condition: service_started
    volumes:
      - ./volumes/worker/logs:/app/logs
    command: worker

  frontend:
    build:
      context: ..
      dockerfile: docker/Dockerfile.frontend
    image: police_training_frontend:latest
    container_name: police-training-frontend
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_started
    ports:
      - "${FRONTEND_PORT:-9090}:80"
