# AI排课自然语言转规则配置实施报告

## 一、定位调整

本次将“AI直接排课”收敛为“AI解析自然语言，规则引擎执行排课”的建议型方案：

- 用户先输入自然语言排课要求
- AI将自然语言解析为结构化排课配置
- 规则引擎按配置生成主方案、备选方案和冲突清单
- 人工审核、调整后再确认应用

这样既体现自然语言能力，又避免大模型直接决定最终课表。

## 二、已完成能力

### 1. 系统默认规则

系统配置已新增培训排课默认规则，支持设置：

- 单课时分钟数
- 课间休息分钟数
- 单节最多课时
- 单日最多课时
- 默认排课方式
- 拆分策略
- 可排课时间段

作用：

- 作为新培训班的默认排课模板
- 作为 AI 排课和手工排课的基础规则来源

### 2. 培训班规则标签页

培训班详情页已新增“排课规则”标签页，可单独维护本班规则：

- 课时单元制
- 时间段配置
- 课间休息
- 单节最多课时
- 默认排课方式

作用：

- 培训班形成自己的正式排课规则
- AI排课优先读取培训班规则
- 后续手工维护课次也按这套规则换算课时

### 3. AI自然语言转配置

AI排课任务页已新增自然语言输入区，并支持将自然语言解析为结构化配置。当前支持识别的重点信息包括：

- 排课范围
- 排课目标
- 排课方式
- 单日最大课时
- 是否避开考试
- 单课时分钟数
- 课间休息分钟数
- 单节最多课时
- 可排课时间段
- 固定课程键

解析方式采用“双层兜底”：

- 优先使用大模型解析
- 大模型不可用或解析失败时，回退到规则匹配解析

### 4. 任务级规则覆盖

AI排课任务页已新增任务级规则覆盖区，可对本次任务临时覆盖：

- 单课时分钟数
- 课间休息分钟数
- 单节最多课时
- 可排课时间段

作用：

- 只影响当前任务
- 不会直接改动培训班正式规则

### 5. 规则引擎升级

排课执行逻辑已从“按固定时间段塞课”升级为“按排课规则切分可排单元后再排课”，核心变化包括：

- 支持课时单元制
- 支持课间休息时长
- 支持自定义可排课时间段
- 支持单节最多课时
- 支持尽量平分拆分策略

拆分策略示例：

- 4课时拆为 2 + 2
- 5课时拆为 3 + 2
- 7课时拆为 3 + 2 + 2

### 6. 手工排课口径统一

培训班详情页手工维护课次时，课时数已不再按自然小时直接计算，而是按培训班排课规则换算：

- 编辑课次时会按“课时单元 + 课间休息”自动换算课时
- 兼容旧数据展示时，也会尽量按当前规则恢复课时数
- AI任务详情中手工修改课次时段，也会同步刷新课时数

## 三、当前效果

经过本轮改造，AI排课能力已经形成以下链路：

1. 系统配置设定全局默认规则
2. 培训班可维护自己的排课规则
3. 用户可用自然语言描述排课要求
4. AI将自然语言转成结构化配置
5. 用户可继续补充或覆盖任务级规则
6. 规则引擎按配置生成方案
7. 人工确认后应用到正式课表

## 四、当前边界

本次已经完成主链路，但仍有边界需要后续继续增强：

- 课次内部子时段目前仍以单个时间范围表达，尚未展开为更细的“子课时明细”
- 自然语言解析已支持常见规则，但复杂业务约束仍建议结合结构化字段确认
- 旧历史课次如果没有保存课时数，只能按当前规则近似恢复

## 五、后续建议

建议下一阶段继续做三件事：

### 1. 增加课次子时段表达

例如一节课 3 个课时，可进一步展示为：

- 08:30-09:10
- 09:20-10:00
- 10:10-10:50

这样更利于课表展示、签到和后续统计。

### 2. 增强自然语言解析模板

补充更多高频表达识别，例如：

- 上午只排理论、下午只排实操
- 周三下午不排课
- 考前一周优先强化某类课程
- 某些教官或场地固定不可用

### 3. 增加规则预览与解释

在 AI 生成前，先展示“本次解析后的结构化排课规则预览”，方便用户确认：

- AI理解了什么
- 哪些规则来自培训班默认
- 哪些规则来自本次任务覆盖

## 六、结论

本次改造后，AI排课已不再只是“一个带 AI 名字的规则页面”，而是具备了清晰的自然语言入口、可配置规则模型和人工确认机制，能够更自然地体现智能化能力，同时仍保持业务可控。
