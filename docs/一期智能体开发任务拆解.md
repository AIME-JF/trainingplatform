# 一期智能体开发任务拆解

## 1. 文档定位

本文档基于《一期智能体可行性研究与后续安排》进一步收敛，目标不是讨论“值不值得做”，而是回答“接下来怎么做、谁来做、做到什么算完成”。

适用范围：

- 一期范围内的 `智慧排课智能体`
- 一期范围内的 `个性化训练智能体`
- 仅覆盖“建议型智能体”
- 不覆盖二期智能体平台化、无人值守执行、外部系统联动

## 2. 拆解原则

- 先做能形成业务闭环的任务，不先做平台化重构
- 先做 `智慧排课智能体`，再做 `个性化训练智能体`
- 优先复用现有 `AITask`、培训、推荐、权限和变更留痕能力
- 每个任务必须有明确交付物和验收标准
- 一期不追求“最优算法”，先保证“可用、可解释、可确认”

## 3. 里程碑定义

| 里程碑 | 时间 | 目标 | 完成标志 |
| --- | --- | --- | --- |
| M0 | 第 1 周 | 方案冻结 | 任务字段、结果结构、标签口径、演示路径冻结 |
| M1 | 第 3 周 | 排课闭环可演示 | 可创建排课任务、生成建议、人工确认并回写课表 |
| M2 | 第 5 周 | 个训闭环可演示 | 可生成个训方案、确认保存快照、按权限查看 |
| M3 | 第 6 周 | 试点验收 | 真实数据联调完成，演示脚本和验收清单完成 |

## 4. 总体任务总览

| 编号 | 任务 | 优先级 | 主要负责人 | 预估人天 | 前置依赖 |
| --- | --- | --- | --- | --- | --- |
| G-01 | 任务协议与口径冻结 | P0 | 产品 + 后端 + AI | 2 | 无 |
| G-02 | AI 任务流通用化改造 | P0 | 后端 | 3 | G-01 |
| S-01 | 排课数据模型与迁移 | P0 | 后端 | 3 | G-01 |
| S-02 | 排课规则引擎与冲突检测 | P0 | 后端 + AI | 5 | S-01 |
| S-03 | 排课结果生成与说明文案 | P0 | 后端 + AI | 3 | S-02 |
| S-04 | 排课接口、权限、确认应用 | P0 | 后端 | 3 | S-02,S-03 |
| S-05 | 排课前端入口与任务页 | P0 | 前端 | 5 | S-04 |
| S-06 | 排课联调与验收 | P0 | 前端 + 后端 + 测试 | 2 | S-05 |
| P-01 | 个训画像聚合与标签口径落地 | P0 | 后端 + AI | 4 | G-01 |
| P-02 | 个训方案生成与资源推荐联动 | P0 | 后端 + AI | 4 | P-01 |
| P-03 | 个训快照、接口与权限 | P0 | 后端 | 3 | P-02 |
| P-04 | 个训前端展示与确认 | P0 | 前端 | 4 | P-03 |
| P-05 | 个训联调与验收 | P0 | 前端 + 后端 + 测试 | 2 | P-04 |
| R-01 | 演示数据、试点与汇报材料 | P0 | 产品 + 测试 + 实施 | 2 | S-06,P-05 |

## 5. 详细任务拆解

## 5.1 公共基础任务

### G-01 任务协议与口径冻结

**目标**

- 冻结一期任务表单、结果结构、约束字段和个训标签口径
- 避免研发阶段边做边改字段，导致前后端反复返工

**涉及模块**

- 产品方案
- 后端 schema
- 前端表单
- AI 提示词输入输出

**建议落点**

- `backend/app/schemas/ai.py`
- `frontend/src/api/ai.js`
- 新增内部字段说明文档或接口草案

**交付物**

- 排课任务请求字段清单
- 排课结果 JSON 结构
- 个训画像标签口径
- 个训方案 JSON 结构
- 演示场景清单

**验收标准**

- 前后端、AI 三方对任务字段和结果结构达成一致
- 字段清单能直接指导接口和页面开发
- 不再存在“先写代码、后补字段定义”的空白项

### G-02 AI 任务流通用化改造

**目标**

- 在保留现有 `AITask` 体系的前提下，支持排课和个训任务
- 避免继续把考试域字段硬塞给训练域

**涉及模块**

- AI 任务模型
- AI 服务层
- Celery 调度
- AI 路由层

**建议文件落点**

- 修改：`backend/app/models/ai_task.py`
- 修改：`backend/app/services/ai.py`
- 修改：`backend/app/views/ai.py`
- 新增：`backend/app/tasks/ai_schedule.py`
- 新增：`backend/app/tasks/ai_personal_training.py`

**交付物**

- 新任务类型枚举：`schedule_generation`
- 新任务类型枚举：`personal_training_plan_generation`
- 任务列表/详情/结果更新/确认的通用处理约定
- 新任务的异步调度入口

**验收标准**

- 新任务类型可正常创建、查询、更新状态
- 不依赖 `confirmed_question_ids`、`confirmed_paper_id` 承载训练域结果
- 现有考试 AI 任务不回归

## 5.2 智慧排课智能体任务

### S-01 排课数据模型与迁移

**目标**

- 补齐排课智能体的一期数据承载结构
- 保证任务可按培训班检索，确认后可留痕

**涉及模块**

- 数据模型
- Alembic 迁移
- 训练域模型导出

**建议文件落点**

- 新增：`backend/app/models/ai_schedule_task.py`
- 修改：`backend/app/models/__init__.py`
- 新增：`backend/alembic/versions/*_add_ai_schedule_tasks.py`

**推荐字段**

- `ai_task_id`
- `training_id`
- `scope_type`
- `goal`
- `constraint_payload`
- `result_snapshot`
- `confirmed_by`
- `confirmed_at`

**交付物**

- 排课任务扩展表
- 关联培训班的任务查询能力
- 迁移脚本

**验收标准**

- 可按 `training_id` 查询排课任务
- 结果快照可回溯
- 迁移脚本能完整创建结构

### S-02 排课规则引擎与冲突检测

**目标**

- 先把“不能排错”做出来，再追求“排得更优”

**涉及模块**

- 培训服务
- 规则引擎
- 课次冲突检测

**建议文件落点**

- 新增：`backend/app/services/schedule_agent.py`
- 新增：`backend/app/services/schedule_rule_engine.py`
- 复用：`backend/app/services/training.py`
- 复用：`backend/app/services/training_course_change.py`

**必须覆盖的硬约束**

- 不超出培训周期
- 已开始/已结束课次不可改
- 教官不可撞时间
- 课次不可重叠
- 考试时段不可冲突

**建议覆盖的软约束**

- 理论/实操比例
- 重点课程前置
- 单教官连续授课不过载
- 地点集中度

**交付物**

- 冲突检测结果结构
- 主方案生成器
- 备选方案生成器

**验收标准**

- 对一组给定输入能返回主方案、备选方案和冲突清单
- 硬冲突能稳定被识别
- 冲突结果可直接被前端展示

### S-03 排课结果生成与说明文案

**目标**

- 在规则引擎产出结构化结果后，由模型生成解释性内容，而不是让模型直接拍板排课

**涉及模块**

- Prompt 构建
- 大模型调用
- 结果结构化输出

**建议文件落点**

- 新增：`backend/app/services/schedule_prompt_builder.py`
- 新增：`backend/app/services/schedule_result_formatter.py`

**交付物**

- 方案说明
- 冲突原因说明
- 调课建议文案
- 主方案与备选方案摘要

**验收标准**

- 说明文案不与结构化结果矛盾
- 同一任务详情中可同时看到结构化课表和说明文字
- 模型失败时规则结果仍可返回

### S-04 排课接口、权限、确认应用

**目标**

- 完成排课任务的后端闭环

**涉及模块**

- 路由
- schema
- 权限校验
- 培训课程更新

**建议文件落点**

- 修改：`backend/app/schemas/ai.py`
- 修改：`backend/app/views/ai.py`
- 修改：`backend/app/services/ai.py`
- 复用：`backend/app/services/training.py`
- 复用：`backend/app/utils/authz.py`

**接口范围**

- `POST /ai/schedule-tasks`
- `GET /ai/schedule-tasks`
- `GET /ai/schedule-tasks/{id}`
- `PUT /ai/schedule-tasks/{id}/result`
- `POST /ai/schedule-tasks/{id}/confirm`

**交付物**

- 完整的后端接口
- 培训管理员/班主任权限控制
- 确认后回写 `TrainingCourse.schedules`
- 应用后写入 `TrainingCourseChangeLog`

**验收标准**

- 任务创建到确认应用能全链路跑通
- 非授权用户不能创建或确认
- 确认应用后现有签到签退链路可继续使用

### S-05 排课前端入口与任务页

**目标**

- 在现有培训页面中提供最少改动、最直观的 AI 排课入口

**涉及模块**

- 培训详情页
- 周训练计划页
- AI 任务 API
- 路由与权限

**建议文件落点**

- 修改：`frontend/src/api/ai.js`
- 修改：`frontend/src/router/index.js`
- 修改：`frontend/src/views/training/Detail.vue`
- 修改：`frontend/src/views/training/Schedule.vue`
- 新增：`frontend/src/views/training/AiScheduleTask.vue`

**交付物**

- 创建排课任务表单
- 排课任务列表
- 排课任务详情
- 方案预览、人工微调、应用确认

**验收标准**

- 用户可从培训详情页或周计划页进入 AI 排课
- 任务详情可展示主方案、备选方案、冲突清单和说明
- 人工调整后可重新提交结果并确认应用

### S-06 排课联调与验收

**目标**

- 完成排课智能体的一期验收闭环和演示路径

**涉及模块**

- 前后端联调
- 数据准备
- 场景验收

**交付物**

- 演示培训班数据
- 验收清单
- 操作手册草稿

**验收标准**

- 能完成“创建任务 -> 查看方案 -> 人工微调 -> 确认应用 -> 查看留痕”完整流程
- 至少覆盖新班初排、临时调课、冲突扫描 3 个场景

## 5.3 个性化训练智能体任务

### P-01 个训画像聚合与标签口径落地

**目标**

- 用现有训历、考试、学习、资源行为数据生成一期可用画像

**涉及模块**

- 训历
- 学习进度
- 考试记录
- 推荐行为

**建议文件落点**

- 新增：`backend/app/services/training_portrait_aggregator.py`
- 复用：`backend/app/services/recommendation.py`
- 复用：`backend/app/services/training.py`

**一期建议标签**

- 理论薄弱
- 实操薄弱
- 出勤风险
- 学习进度滞后
- 资源偏好方向
- 考前强化需求

**交付物**

- 画像聚合服务
- 标签计算规则
- 画像快照结构

**验收标准**

- 对同一学员可稳定生成结构化画像
- 标签来源可追溯到已有业务数据
- 前端或接口可展示画像依据摘要

### P-02 个训方案生成与资源推荐联动

**目标**

- 基于画像生成训练方案，并联动现有推荐服务给出资源建议

**涉及模块**

- 个训方案生成
- 推荐资源整合
- Prompt 构建

**建议文件落点**

- 新增：`backend/app/services/personal_training_plan_agent.py`
- 新增：`backend/app/services/training_plan_prompt_builder.py`
- 复用：`backend/app/services/recommendation.py`

**交付物**

- 个训方案结构
- 推荐课程/资源清单
- 方案说明文案

**验收标准**

- 可生成目标、频次、周期、资源推荐完整方案
- 推荐资源和短板标签之间存在可解释关系
- 模型失败时至少可返回规则计算版方案草稿

### P-03 个训快照、接口与权限

**目标**

- 让个训方案具备可确认、可保存、可历史查看的能力

**涉及模块**

- 数据模型
- 路由接口
- 权限控制

**建议文件落点**

- 新增：`backend/app/models/personal_training_plan_snapshot.py`
- 修改：`backend/app/models/__init__.py`
- 新增：`backend/alembic/versions/*_add_personal_training_plan_snapshots.py`
- 修改：`backend/app/schemas/ai.py`
- 修改：`backend/app/views/ai.py`

**接口范围**

- `POST /ai/personal-training-tasks`
- `GET /ai/personal-training-tasks`
- `GET /ai/personal-training-tasks/{id}`
- `PUT /ai/personal-training-tasks/{id}/result`
- `POST /ai/personal-training-tasks/{id}/confirm`

**交付物**

- 个训方案快照表
- 任务创建、详情、更新、确认接口
- 本人查看和教官查看权限

**验收标准**

- 确认后的方案可作为快照保存
- 同一学员可保留历史版本
- 权限边界符合“本人查看、教官查看所管理学员”

### P-04 个训前端展示与确认

**目标**

- 把个训方案从“后端结果”变成用户可读、可确认、可回看的页面

**涉及模块**

- 培训详情页
- 学员训历页
- AI 任务页

**建议文件落点**

- 修改：`frontend/src/api/ai.js`
- 修改：`frontend/src/router/index.js`
- 修改：`frontend/src/views/training/Detail.vue`
- 修改：`frontend/src/views/training/History.vue`
- 新增：`frontend/src/views/training/AiPersonalTrainingTask.vue`

**交付物**

- 个训任务创建入口
- 个训任务详情页
- 方案确认入口
- 历史方案查看入口

**验收标准**

- 教官可为学员生成方案
- 学员可查看自己的确认方案
- 方案中能看到短板标签、训练计划和资源推荐

### P-05 个训联调与验收

**目标**

- 完成个训智能体的一期验收闭环

**涉及模块**

- 前后端联调
- 数据准备
- 权限校验

**交付物**

- 样例学员数据
- 样例方案结果
- 验收清单

**验收标准**

- 能完成“生成方案 -> 查看依据 -> 确认方案 -> 历史查看”完整流程
- 至少覆盖单学员生成和教官查看学员两类场景

## 5.4 试点与汇报任务

### R-01 演示数据、试点与汇报材料

**目标**

- 把可开发版本变成可汇报版本

**涉及模块**

- 产品
- 测试
- 实施
- 业务数据准备

**交付物**

- 演示培训班、演示学员、演示资源
- 演示脚本
- 验收结论
- 汇报页要点

**验收标准**

- 演示脚本可稳定复现
- 每个智能体至少有 1 条完整业务路径
- 汇报时可以明确展示“AI 建议 + 人工确认 + 留痕生效”

## 6. 建议文件改动清单

## 6.1 后端重点文件

建议优先关注以下文件或目录：

- `backend/app/models/ai_task.py`
- `backend/app/models/__init__.py`
- `backend/app/schemas/ai.py`
- `backend/app/views/ai.py`
- `backend/app/services/ai.py`
- `backend/app/services/training.py`
- `backend/app/services/recommendation.py`
- `backend/app/tasks/`
- `backend/alembic/versions/`

建议新增的主要文件：

- `backend/app/models/ai_schedule_task.py`
- `backend/app/models/personal_training_plan_snapshot.py`
- `backend/app/services/schedule_agent.py`
- `backend/app/services/schedule_rule_engine.py`
- `backend/app/services/schedule_prompt_builder.py`
- `backend/app/services/training_portrait_aggregator.py`
- `backend/app/services/personal_training_plan_agent.py`
- `backend/app/services/training_plan_prompt_builder.py`
- `backend/app/tasks/ai_schedule.py`
- `backend/app/tasks/ai_personal_training.py`

## 6.2 前端重点文件

- `frontend/src/api/ai.js`
- `frontend/src/router/index.js`
- `frontend/src/views/training/Detail.vue`
- `frontend/src/views/training/Schedule.vue`
- `frontend/src/views/training/History.vue`

建议新增的主要文件：

- `frontend/src/views/training/AiScheduleTask.vue`
- `frontend/src/views/training/AiPersonalTrainingTask.vue`

## 7. 一期不纳入开发任务的内容

以下内容明确不纳入本轮拆解，防止范围失控：

- 无人值守自动排课/自动生效
- OA 审批联动
- 全量师资排班中台
- 复杂场地台账系统
- 通用智能体平台重构
- 二期主观题评分、认证考试、购物车式选课

## 8. 最终执行建议

建议研发排期按以下顺序推进：

1. 先完成 `G-01`、`G-02`，把公共协议和任务流打稳
2. 再连续完成 `S-01` 到 `S-06`，优先交付排课闭环
3. 然后完成 `P-01` 到 `P-05`，交付个训闭环
4. 最后做 `R-01`，集中整理演示、试点和验收材料

如果资源不足，优先级应进一步收敛为：

1. `G-01`
2. `G-02`
3. `S-01`
4. `S-02`
5. `S-04`
6. `S-05`
7. `S-06`

也就是先把 `智慧排课智能体` 做成可展示、可确认、可留痕的一条完整链路，再继续推进个训。
