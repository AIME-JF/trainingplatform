# 待实现功能清单

## 1. 文档说明

本文档基于以下两部分信息整理：

- 需求来源：`训练平台需求报告.html`
- 当前代码基线：前端 `frontend/src/views`、`frontend/src/router/index.js`、`frontend/src/api`，后端 `backend/app/views`、`backend/app/models`、`backend/app/schemas`、`backend/app/services`

整理目标不是重复需求报告，而是回答两个问题：

1. 当前项目已经具备了哪些能力
2. 与需求报告相比，哪些功能仍需补齐，应该如何落地

评估时间基线：`2026-03-16`

### 1.1 状态判定口径

- `已实现`：前后端已有明确页面/接口/数据结构，业务流程基本能闭环
- `部分实现`：已有部分页面、接口或数据结构，但与需求报告目标仍有明显差距
- `未实现`：代码中没有对应落点，或仅存在非常弱的概念性能力，无法支撑真实业务

### 1.2 当前项目已具备的基础能力

当前项目并不是空白项目，已经具备以下基础：

- 培训班管理：培训列表、详情、创建更新、开班/结班、报名、报名审核、签到、签到二维码、课表导入、学员/教官/课表 Excel 导入、培训与资源绑定
- 考试基础：题库、考试创建、参加考试、自动阅卷、成绩查看、成绩分析
- 资源中心：资源上传、资源库、审核队列、审核策略、推荐流、媒体上传
- 人员基础：用户、角色、部门、警种、教官基础信息、学员列表、个人中心
- 其他支撑：公告、证书、人才库、数据看板、AI 智能出题、AI 自动组卷、AI 自动生成试卷

这意味着后续工作重点不是“从零搭系统”，而是补齐需求报告中的业务闭环、角色边界和数据沉淀能力。

### 1.3 今日进展快照（2026-03-16）

- 考试系统完成首轮重构：
  - 已拆成 `Question -> ExamPaper -> AdmissionExam / Exam -> Record`
  - 已新增独立试卷管理页面与后端试卷接口
  - 试卷支持 `draft / published / archived`
  - 考试创建已改为只关联 `paper_id`
  - 培训班通过 `admission_exam_id` 关联准入考试
  - 培训班考试已移除 `scope`，参试范围由 `training_id` 决定
  - 准入考试已支持 `all / user / department / role` 结构化适用范围
  - 准入考试已在后端按适用范围做真实拦截
- AI 能力完成首轮任务化重构：
  - 已拆成 `AI 智能出题 / AI 自动组卷 / AI 自动生成试卷`
  - 已统一成“创建任务 -> 查看任务 -> 编辑结果 -> 确认入库”的任务流
  - 当前结果由后端模拟生成
- 培训基地完成首轮全链路落地：
  - 已新增 `TrainingBase` 表、后端服务、接口和前端管理页
  - 培训班已支持通过 `training_base_id` 关联培训基地
- 培训班管理完成首轮 P0 补齐：
  - 已支持发布、锁定名单、开班、结班
  - 培训班流程已按“发布招生 -> 锁定名单 -> 开班”做严格顺序控制
  - 提前点击后续流程节点时，会弹窗确认是否跳过前置环节，并通过 `skip_steps` 显式传给后端
  - 已支持班级编组与班干部设置
  - 培训详情已返回 `workflow_steps`、`current_session`、`exam_sessions`
  - 培训班已支持“报名是否需要申请”，可配置为审批报名或直接通过
  - 培训详情“学员名单”标签已支持内嵌申请管理弹层，班主任 / 管理员可直接审批申请
- 培训班归属字段已补齐：
  - 已支持 `department_id`
  - 已支持 `police_type_id`
  - 已支持 `training_base_id`
  - 选择培训基地时会自动带出地点，并优先继承基地部门
- 学员过程管理完成首轮闭环：
  - 已支持开始 / 结束签到、开始 / 结束签退
  - 已支持评课、自动缺勤补记、训历汇总
- 教官与课程关系完成结构化：
  - 课程支持 `primary_instructor_id`
  - 课程支持 `assistant_instructor_ids`
  - 班主任和课次授课教官的权限边界已按代码落地
- 基础设施补强：
  - 已补兼容迁移修复脚本
  - 自动迁移不再对旧库直接 `stamp head`
  - 权限补齐、`admin` 权限回填和内置角色默认 `data_scopes` 修正已迁入 Alembic 数据迁移
  - 启动前会校验关键表结构兼容性
- 数据范围控制完成首轮落地：
  - 角色管理已支持多选 `all / department / department_and_sub / police_type / self`
  - 用户、培训班、培训基地、题库已接入对象级数据范围控制
  - 题目已支持 `police_type_id`，为按警种题库隔离提供结构基础
  - 试卷仓库和培训班考试列表 / 详情也已接入数据范围过滤
- 前端权限体系完成一轮收口：
  - 侧边栏与移动端菜单已改为按权限显示，不再仅按角色硬编码
  - 试卷仓库已新增独立试卷详情页，支持查看题目答案和解析

## 2. 总体结论

对照需求报告，当前项目的完成情况可以概括为：

- `培训管理一期` 已完成首轮流程化重构，`发布、锁定、严格顺序控制、分组、班干部、当前课次、课次操作` 已有真实落点，但 `解锁审批、完整审计台账` 仍未闭环
- `学员管理一期` 已不再只是“报名 + 签到”，`签退、评课、缺勤补记、训历汇总` 已有落点，但 `年度导出、请假/补签、移动端自服务` 仍是缺口
- `教官管理一期` 只完成了“静态资料展示”层，`教官在线采集、评聘报表` 尚未闭环
- `考试平台一期` 已完成“试卷管理 + 准入考试 + 培训班考试”的首轮重构，`考试与班次绑定、准入制考试、成绩分析、独立试卷运营能力` 已有落点，但 `按警种题库隔离、认证类考试` 仍未完成
- `课程资源库一期` 已有上传、审核、推荐雏形，但 `移动端短视频体验、征集批次、分级审核闭环` 还未按需求报告做成
- `二期功能` 基本都还没有真正启动，包括 `师资库/课程库购物车选课、AI 主观题评分、视频与培训联动、等级认证考试`

## 3. 总览清单

| 模块 | 功能点 | 当前状态 | 优先级 |
| --- | --- | --- | --- |
| 开班管理 | 班次发布与挂网预告 | 部分实现 | P0 |
| 开班管理 | 班级编组、班干部指定 | 部分实现 | P0 |
| 开班管理 | 班次锁定、名单冻结 | 部分实现 | P0 |
| 开班管理 | 培训基地管理与班次归属 | 部分实现 | P0 |
| 学员管理 | 在线报名与统一建档 | 部分实现 | P0 |
| 学员管理 | 签退与评课闭环 | 部分实现 | P0 |
| 学员管理 | 缺勤自动判定与留痕 | 部分实现 | P0 |
| 学员管理 | 训历档案与年度导出 | 部分实现 | P0 |
| 教官管理 | 教官在线采集 | 部分实现 | P0 |
| 教官管理 | 师资库与购物车排课 | 未实现 | P2 |
| 考试平台 | 考试场次与试卷分层重构 | 部分实现 | P0 |
| 考试平台 | 客观题基础能力增强 | 部分实现 | P0 |
| 考试平台 | 准入制考试 | 部分实现 | P0 |
| 考试平台 | 按警种题库隔离 | 部分实现 | P1 |
| 考试平台 | AI 主观题评分 | 未实现 | P2 |
| 考试平台 | 等级认证考试 | 未实现 | P2 |
| 课程资源库 | 视频公共资源库 | 部分实现 | P0 |
| 课程资源库 | 移动端短视频浏览 | 部分实现 | P0 |
| 课程资源库 | 视频征集与分级审核 | 部分实现 | P0 |
| 课程资源库 | 视频与培训联动 | 未实现 | P2 |
| 跨模块 | 角色体系重构 | 部分实现 | P0 |
| 跨模块 | 数据范围控制（用户/培训班/培训基地/题库） | 部分实现 | P0 |
| 跨模块 | OA 审批结果承接 | 未实现 | P1 |
| 跨模块 | 留痕、导出、通知、智能体接入 | 部分实现 | P0/P2 |

## 4. 详细待实现功能

## 4.1 开班管理

### 4.1.1 班次发布与挂网预告

**需求目标**

- 支持培训班信息录入
- 支持挂网预告和发布
- 支持课程表 Excel 导入
- 平台不做 OA 审批流，但要能承接审批完成后的发布结果

**当前状态**

- 已有培训班增删改查、发布、锁定名单、开班/结班、课程表导入能力
- 培训详情已提供步骤条、当前课次、考试安排等聚合信息
- 后端已有 `publish_status`、`published_at`、`published_by`、`locked_at`、`locked_by` 等字段
- 但还没有真正的“外部挂网页面、外部审批回执、发布日志台账”

**差距说明**

- 现在已经不是纯“内部数据管理”，但仍缺外部挂网预告和审批承接闭环
- 没有显式的对外预告页、发布对象、公开范围
- 没有“OA 审批通过后发布”的承接点，无法和需求中的外部审批方式对齐

**建议实现内容**

- 数据模型补充：
  - 培训班发布状态：`draft`、`pending_external_approval`、`approved`、`published`、`locked`、`active`、`ended`
  - 发布信息：发布时间、发布人、发布范围、外部审批单号、审批结果同步时间
- 后端接口补充：
  - 发布培训班
  - 取消发布
  - 同步外部审批结果
  - 查询发布记录
- 前端页面补充：
  - 培训班发布面板
  - 对外预告列表/挂网页面
  - 发布日志抽屉
- 业务规则补充：
  - 未发布班次不可对外报名
  - 已发布班次才进入报名阶段
  - 外部审批失败时保留退回原因

**优先级**

- `P0，一期必须补齐`

### 4.1.2 班级编组与班干部指定

**需求目标**

- 支持班次内分组
- 支持指定班长、学习委员等班干部
- 支持按组查看学员

**当前状态**

- `Enrollment` 已包含 `group_name`、`cadre_role`
- 已有 `PUT /trainings/{training_id}/roster` 接口更新编组和班干部
- 培训详情接口已返回 `group_names`、`cadre_count`

**差距说明**

- 已能满足基础分组与班干部设置，但仍缺独立的组对象与分组报表
- 还无法支撑后续分组考核、分组签到、分组统计
- 训历和导出报表中尚未完整体现班内职责

**建议实现内容**

- 数据模型新增：
  - `training_groups`
  - `training_group_members`
  - `training_class_roles`
- 页面补充：
  - 班级编组管理页
  - 班干部任命面板
  - 分组导入/导出
- 后端能力：
  - 手动分组
  - 按单位/警种/性别/成绩预分组
  - 班干部任命/撤销
- 报表能力：
  - 导出分组名单
  - 导出班干部名单

**优先级**

- `P0，一期必须补齐`

### 4.1.3 班次锁定与名单冻结

**需求目标**

- 报名截止后锁定班次名单
- 锁定后仅允许锁定名单参与签到、签退、考试、证书生成等流程

**当前状态**

- `Training` 已有 `locked_at`、`locked_by`
- 已有 `POST /trainings/{training_id}/lock` 锁定名单接口
- 锁定后会拒绝继续调整学员名单，并处理待审核报名
- 但仍没有独立的“锁定名单快照”表和解锁流程

**差距说明**

- 锁定动作已经落地，但仍缺显式快照、解锁审批和锁定历史台账
- “仅锁定名单参与后续流程”当前主要依赖已审批名单和禁止修改，而不是单独的快照机制

**建议实现内容**

- 数据模型新增：
  - 锁定标志
  - 锁定时间
  - 锁定人
  - 锁定名单快照
- 后端规则：
  - 锁定后禁止新增/变更报名结果
  - 锁定后签到、签退、考试、证书仅校验锁定名单
  - 支持解锁，但必须记录原因
- 前端页面：
  - 锁定操作入口
  - 解锁审批/确认入口
  - 锁定历史查看

**优先级**

- `P0，一期必须补齐`

### 4.1.4 培训基地管理与班次归属

**需求目标**

- 独立维护培训基地
- 培训班可选择培训基地
- 培训班可维护部门、警种归属，用于后续数据域管理

**当前状态**

- 已新增独立 `TrainingBase` 模型、服务、接口和前端页面
- 培训基地支持 `name`、`location`、`department_id`、`description`
- 培训班已支持 `department_id`、`police_type_id`、`training_base_id`
- 培训班选择培训基地时，地点会自动带出；如果培训基地配置了部门，会优先继承该部门

**差距说明**

- 首轮结构和页面已经落地，但还没有形成“基地维度统计、基地运营台账、基地承训能力分析”
- 当前部门和警种字段主要用于数据域控制，尚未用于看板、报表和更细的业务规则

**建议实现内容**

- 增强基地维度统计：
  - 培训基地承训班次数
  - 培训基地承训人数
  - 基地课程结构和使用频次
- 增强培训班归属能力：
  - 导出和报表统一带出基地、部门、警种
  - 支持按基地筛选班次
  - 后续承训单位角色可基于基地归属展开

**优先级**

- `P0，一期已完成首轮落地，后续继续增强`

## 4.2 学员管理

### 4.2.1 在线报名与统一建档

**需求目标**

- 学员通过 PC 或移动端报名
- 复用既有账号信息
- 报名结果自动汇总到班次

**当前状态**

- 已有报名页面、报名管理页面、报名审核接口
- 已有用户、部门、警种等基础数据结构
- 培训班已支持配置“报名是否需要申请”，可直接通过或进入待审核
- 培训详情页已内嵌申请管理弹层，不再必须跳转独立审核页

**差距说明**

- 目前报名流程偏“单点报名”，不是“统一建档”
- 未见与外部警务基础信息系统的真实对接能力
- 不支持按培训班配置扩展报名字段、附件、承诺书等

**建议实现内容**

- 报名表单配置化：
  - 培训班可定义必填项、附件项、只读项
- 数据同步：
  - 对接外部人员基础信息源
  - 支持首次同步、定时同步、手动刷新
- 报名数据归档：
  - 保存报名快照，避免用户后续资料变更影响历史记录
- 移动端支持：
  - 学员移动端报名页
  - 报名结果页

**优先级**

- `P0，一期必须补齐`

### 4.2.2 签退与评课闭环

**需求目标**

- 学员签到
- 学员签退
- 签退时触发评课
- 形成课程评价数据

**当前状态**

- 已实现开始 / 结束签到、开始 / 结束签退
- 已有签退接口、评课接口和签到记录中的签退 / 评课字段
- 培训详情页已围绕当前课次提供快捷操作
- 结束签到后课次状态会进入 `checkin_closed`，当前页面按“进行中”展示，而不是“签到已结束”

**差距说明**

- 基础闭环已经具备，但评课仍未沉淀为完整统计报表
- 还缺少“签退强制评课、评课模板、评课排行”等运维能力

**建议实现内容**

- 数据模型新增：
  - `checkout_records`
  - `course_evaluations`
- 业务规则：
  - 签退与课程场次绑定
  - 签退前可校验签到状态
  - 评课可设为签退前必填或签退后限时补填
- 页面与接口：
  - 学员签退页
  - 学员评课表单
  - 管理端评课统计页
- 报表输出：
  - 教官评分统计
  - 课程满意度排行

**优先级**

- `P0，一期必须补齐`

### 4.2.3 缺勤自动判定与异常留痕

**需求目标**

- 根据签到数据自动标记缺勤
- 区分正常、迟到、缺勤、请假等状态
- 形成异常留痕

**当前状态**

- `CheckinRecord` 已有 `on_time/late/absent` 状态
- 课次超过截止时间后会自动转为 `missed`，并自动补记缺勤记录
- `checkin_closed` 的业务语义是“课程仍在进行，但签到窗口已结束”
- 记录里已包含 `absence_reason`
- 但还没有请假例外、补签审批和异常修正流程

**差距说明**

- 当前“缺席”更像展示字段，不是完整业务闭环
- 无法满足真实培训管理中的请假、补签、人工修正

**建议实现内容**

- 规则引擎：
  - 每个签到场次设置签到窗口
  - 超时未签到自动生成缺勤
  - 支持请假、外训、调课等例外状态
- 审计能力：
  - 记录补签人、补签原因、补签时间
  - 保留原始状态和修正状态
- 提醒能力：
  - 班次异常预警
  - 学员连续缺勤预警

**优先级**

- `P0，一期必须补齐`

### 4.2.4 学员训历档案与年度导出

**需求目标**

- 自动沉淀参训记录
- 关联考试成绩
- 支持年度训练分析
- 支持一键导出年度材料

**当前状态**

- 已有 `TrainingHistory` 及 `/trainings/{training_id}/histories`、`/trainings/histories/me`
- 训历会汇总培训、签到、考试通过数等字段
- 但还没有“年度考核导出模板”和完整的年度视图

**差距说明**

- 需求里的训历是纵向个人档案，不是横向看板统计
- 当前考试、培训、签到、证书数据没有统一汇总到个人训历

**建议实现内容**

- 训历聚合视图：
  - 参训班次
  - 报名记录
  - 签到签退
  - 考试成绩
  - 证书情况
  - 评课与表现
- 页面能力：
  - 个人训历页
  - 管理员按单位/警种查看训历页
- 导出能力：
  - 年度训练记录导出
  - 年度考核表导出
  - 单人训历 PDF/Excel 导出

**优先级**

- `P0，一期必须补齐`

### 4.2.5 学员移动端自服务

**需求目标**

- 学员可在移动端完成报名、签到签退、评课、查看训历

**当前状态**

- 当前移动端只有签到相关页面
- 报名、签退、评课、训历查询还没有移动端闭环

**差距说明**

- 与需求报告中“移动端适配”目标不一致

**建议实现内容**

- 新增移动端页面：
  - 我的报名
  - 我的培训
  - 签退与评课
  - 我的训历
  - 我的证书

**优先级**

- `P1，一期可分批补齐`

## 4.3 教官管理

### 4.3.1 教官在线信息采集

**需求目标**

- 教官在线填写个人信息
- 字段可维护
- 一键提交

**当前状态**

- 用户模型已包含大量教官扩展字段
- 前端已有教官列表和详情页
- 但更像管理员查看资料，不是“教官本人在线采集”

**差距说明**

- 缺少采集流程、提交状态、审核状态
- 缺少“字段模板管理”，无法按业务阶段调整采集项

**建议实现内容**

- 数据层：
  - 教官资料草稿
  - 教官资料提交记录
  - 教官附件材料
- 管理层：
  - 采集模板配置
  - 字段启停、必填、展示顺序
- 前端能力：
  - 教官端资料填写页
  - 管理端资料审核页

**优先级**

- `P0，一期必须补齐`

### 4.3.2 教官评聘报表与数据分析

**需求目标**

- 用系统记录支撑教官评聘
- 导出授课记录、授课班次、授课时长等客观数据

**当前状态**

- 当前项目有教官字段，也有人才库
- 但没有真正的授课记录台账和评聘报表

**差距说明**

- 人才库不能替代“教官授课履历”
- 评聘需要的是经过班次和授课行为沉淀出来的数据

**建议实现内容**

- 增加统计报表：
  - 年度授课总时长
  - 授课班次数量
  - 课程类型分布
  - 学员评课均分
- 导出模板：
  - 教官竞聘佐证材料
  - 厅级教官评聘报表

**优先级**

- `P1，一期后半段或二期初优先完成`

### 4.3.3 师资库与购物车式排课

**需求目标**

- 全区师资检索
- 从师资库/课程库选课
- 购物车式组合后生成课表

**当前状态**

- 当前只有教官列表与详情浏览能力
- 没有师资库、课程库、购物车式排课模型

**差距说明**

- 这是明确的二期功能，当前尚未启动

**建议实现内容**

- 二期新增：
  - 师资库
  - 课程库
  - 候选篮/购物车
  - 自动生成课表
  - 授课冲突检测

**优先级**

- `P2，二期规划`

## 4.4 考试平台

### 4.4.1 考试场次与试卷分层重构

**需求目标**

- 在线考试既支持准入制考试，也支持班内考核
- 多种考试形态共用一套题库
- 同一张试卷可以复用于不同考试场次
- 历史考试结果不能因题库后续改题而被污染

**当前状态**

- 当前系统已拆成 `Question -> ExamPaper -> AdmissionExam / Exam -> AdmissionExamRecord / ExamRecord`
- `ExamPaper` 已固化题目快照，支持同题库出卷
- 已新增独立试卷管理页面和后端试卷接口
- 试卷管理已新增独立详情页，详情接口会返回每道题的标准答案和解析所需字段
- 试卷已具备 `draft / published / archived` 状态流
- 考试创建和更新已改为只关联 `paper_id`，不再在考试页直接抽题
- `AdmissionExam` 用于独立准入考试，`Exam` 用于培训班内考试
- 培训班详情接口会返回 `exam_sessions`，培训班通过 `admission_exam_id` 关联准入考试
- 培训班考试已具备 `training_id`、`purpose`、`allow_makeup`
- 试卷仓库和培训班考试列表 / 详情已接入数据范围过滤

**差距说明**

- 首轮分层和试卷独立运营已经落地，但更细粒度的考试运营能力仍有缺口
- 题库仍未按警种做隔离
- 认证类考试、复杂补考策略和更强的考试运营台账仍未完成

**建议实现内容**

- 保留统一题库原则：
  - 全平台共用一套 `Question` 题库
  - 题库按警种、知识点、用途、难度、可见范围做标签和权限隔离
- 结构拆分为四层：
  - `Question`：题库题目
  - `Paper`：试卷定义，从题库选题生成
  - `ExamSession`：考试场次，描述“何时、给谁、因何而考”
  - `ExamAttempt`：作答记录
- 试卷层要求：
  - 支持手工选题、规则组卷
  - 发布后固化题目快照，不直接依赖题库实时内容
  - 允许一张试卷复用到多个场次
- 场次层要求：
  - 增加 `purpose` 字段，区分 `admission`、`class_assessment`、`final_assessment`、`certification`
  - 增加 `training_id` 或 `target_training_id`
  - 增加参考范围、允许次数、是否补考、场次状态
- 作答层要求：
  - 一个学员可在同一场次下有一条或多条作答记录
  - 支持缺考、重考、补考、豁免

**优先级**

- `P0，一期必须补齐`

### 4.4.2 客观题考试基础能力增强

**需求目标**

- 支持单选、多选、判断
- 自动阅卷
- 考试与班次关联
- 成绩进入学员训历

**当前状态**

- 题库和考试基础流程已实现
- 题型已支持单选、多选、判断
- 自动阅卷、成绩查看、分析已存在
- 已完成“试卷快照 + 准入考试 + 培训班考试”的正式分层
- 考试管理页已改成选择已发布试卷，不再直接抽题
- 培训详情已能查看培训班考试摘要

**差距说明**

- 当前考试体系仍是独立运行的
- “在线考试能力”已经有了，但“班内考核能力”没有真正落地
- 成绩虽然可查，但还没有沉淀为培训过程中的正式考核节点

**建议实现内容**

- 数据结构补充：
  - 基于 `Paper + ExamSession + ExamAttempt` 进行扩展
  - 考试用途、培训关联、考试阶段、成绩回写标志
- 业务补充：
  - 班次详情中查看关联考试
  - 班次下可配置多场班内考核
  - 成绩自动回写学员训历
  - 按班次查看考试完成率

**优先级**

- `P0，一期必须补齐`

### 4.4.3 准入制考试

**需求目标**

- 创建班次时可绑定准入考试
- 学员通过准入考试后才允许报名
- 通过名单自动进入可报名范围

**当前状态**

- 当前已存在独立的 `AdmissionExam`
- 培训班已支持 `admission_exam_id`
- 报名逻辑已校验准入考试通过记录

**差距说明**

- 核心“先考后报”链路已具备
- 但仍缺少豁免、批量导入通过名单、准入名单运营面板等增强能力

**建议实现内容**

- 数据模型新增：
  - 准入考试场次配置
  - 准入通过名单缓存/快照
- 报名规则新增：
  - 若班次绑定准入考试，则报名前必须通过
  - 支持手工导入已通过名单
  - 支持豁免配置
- 页面新增：
  - 班次绑定准入考试
  - 准入名单管理
  - 学员报名资格提示

**优先级**

- `P0，一期必须补齐`

### 4.4.4 按警种维护题库与数据权限隔离

**需求目标**

- 各总队维护本警种题库
- 题库、考试、成绩按警种和权限隔离

**当前状态**

- 当前系统已有警种管理、用户警种关联
- 题库题目已支持 `police_type_id`
- 题库列表、创建、更新、批量导入已接入按警种的数据范围控制
- 但考试、试卷还没有形成更完整的警种隔离和运营能力

**差距说明**

- 题目级别的警种字段已经补上，但试卷和考试场次层面仍缺更完整的警种治理
- 当前已经具备“统一题源 + 按警种做基础隔离”的结构基础，但还没有做成完整的警种题库运营体系

**建议实现内容**

- 模型补充：
  - 题目关联警种
  - 题目关联用途、知识点、可见范围
  - 试卷关联警种/适用范围
- 权限规则：
  - 总队管理员只能维护本警种题库
  - 厅级管理员可查看全局
- 页面改造：
  - 题库页面按警种过滤
  - 组卷时按警种限定题源
- 快照规则：
  - 共用统一题库出卷
  - 历史考试基于试卷快照存档，不受题库后续改题影响

**优先级**

- `P1，一期中后段可补齐`

### 4.4.5 AI 主观题评分

**需求目标**

- 支持主观题
- 支持 AI 自动评分
- 适配不同警种的评分标准

**当前状态**

- 当前 AI 能力已经重构为任务化的智能出题、自动组卷、自动生成试卷
- 考试模型中仍然没有主观题结构和主观题评分流程

**差距说明**

- 二期核心能力未启动

**建议实现内容**

- 二期新增：
  - 主观题题型
  - 主观题答案存储
  - AI 评分结果
  - 人工复核/仲裁
  - 评分提示词与警种评分模板

**优先级**

- `P2，二期规划`

### 4.4.6 警种等级认证考试

**需求目标**

- 支持更长期的等级认证类考试
- 形成等级认证记录

**当前状态**

- 当前考试仍是普通培训考试模型
- 无等级、证书级别、认证周期等能力

**建议实现内容**

- 二期新增：
  - 认证考试类型
  - 认证等级
  - 有效期与复评周期
  - 认证结果与证书联动

**优先级**

- `P2，二期规划`

## 4.5 课程资源库

### 4.5.1 视频公共资源库

**需求目标**

- PC 端公开视频资源浏览
- 支持视频资源上架
- 支持一定规模的公共资源库运营

**当前状态**

- 资源上传、资源库、审核、推荐、详情页已存在
- 已有媒体上传能力

**差距说明**

- 当前资源中心更偏“通用资源系统”
- 与需求报告中的“视频公共资源库运营”相比，仍缺少征集批次、栏目运营、展示位策略等能力

**建议实现内容**

- 新增资源运营能力：
  - 首页栏目配置
  - 视频专题
  - 征集批次
  - 推荐位管理
- 新增内容指标：
  - 浏览量
  - 完播率
  - 收藏/点赞（如后续需要）

**优先级**

- `P0，一期必须补齐`

### 4.5.2 移动端短视频浏览体验

**需求目标**

- 移动端支持类短视频上下滑动浏览

**当前状态**

- 当前已有资源推荐流页面
- 但未见专门的移动端沉浸式短视频浏览路由和交互

**差距说明**

- 目前更像常规列表/推荐页，不是需求中强调的“类短视频”体验

**建议实现内容**

- 前端新增：
  - 移动端沉浸式视频页
  - 上下滑切换视频
  - 自动续播/预加载
  - 竖屏适配
- 后端补充：
  - 推荐流分页
  - 视频热度排序
  - 完播行为上报

**优先级**

- `P0，一期必须补齐`

### 4.5.3 视频征集与分级审核闭环

**需求目标**

- 下发征集通知
- 各市政治部初审
- 厅级终审
- 审核通过后上架

**当前状态**

- 当前系统已有通用审核策略、审核任务、审核队列
- 也有公告模块、资源审核模块

**差距说明**

- 当前是“通用审核引擎雏形”，不是“面向视频征集业务的流程闭环”
- 未体现“征集批次、初审单位、终审单位、审核意见留痕、上架批次”这些业务对象

**建议实现内容**

- 业务对象新增：
  - 征集活动/征集批次
  - 投稿记录
  - 初审记录
  - 终审记录
  - 上架记录
- 角色映射：
  - 市级初审角色
  - 厅级终审角色
- 页面能力：
  - 征集通知管理
  - 投稿审核台
  - 上架发布台

**优先级**

- `P0，一期必须补齐`

### 4.5.4 视频与培训联动

**需求目标**

- 开班前指定预习视频
- 看视频后做考测
- 形成“预习 -> 考测 -> 正式参训”闭环

**当前状态**

- 当前虽有培训与资源绑定能力
- 但没有“预习任务、观看进度、完成校验、视频后考测”等闭环

**差距说明**

- 这部分与需求报告明确一致，当前属于未实现

**建议实现内容**

- 二期新增：
  - 班次预习资源配置
  - 学员观看进度
  - 视频后测验
  - 完成条件校验
  - 未完成预习预警

**优先级**

- `P2，二期规划`

## 4.6 跨模块基础能力

### 4.6.1 角色体系与数据权限重构

**需求目标**

- 角色至少覆盖：厅级管理员、各总队管理员、承训单位、学员、教官

**当前状态**

- 当前前端和后端的业务角色仍主要按 `admin/instructor/student` 使用
- 角色管理已支持配置 `all / department / department_and_sub / police_type / self`
- 用户、培训班、培训基地、题库已接入对象级数据范围控制
- 培训班新增 `created_by`，并与班主任一起计入“本人”范围

**差距说明**

- 数据范围和对象级权限已经有了首轮基础，但需求报告中的“厅级 / 总队 / 承训单位”业务角色还未完成
- 菜单、报表、导出、资源库等更多模块还没有全面接入这套数据范围规则
- 角色名称、菜单范围、数据范围和需求报告仍未完全对齐

**建议实现内容**

- 先做角色映射表
- 再做菜单、接口、数据三层权限统一
- 最终目标是“角色看到什么、能操作什么、能看哪些数据”一致

**优先级**

- `P0，一期必须补齐`

### 4.6.1A 数据范围控制（用户 / 培训班 / 培训基地 / 题库）

**需求目标**

- 角色可配置多种数据范围
- 不同业务对象按自身是否具备部门、警种字段实施对象级数据控制

**当前状态**

- 角色管理已支持多选数据范围：
  - `all`
  - `department`
  - `department_and_sub`
  - `police_type`
  - `self`
- 当前已接入数据范围控制的对象：
  - 用户
  - 培训班
  - 培训基地
  - 题库题目
- 用户对象当前按“部门或警种任一命中即可”控制
- 培训班对象当前按“部门和警种同时满足”控制；`created_by` 和 `instructor_id` 同时计入“本人”

**差距说明**

- 资源库、报表、更多系统管理对象还未接入这套数据范围控制
- 业务角色和菜单层仍未完全围绕数据范围重构
- 还没有形成统一的数据范围说明页和运维配置台账

**建议实现内容**

- 扩展接入对象：
  - 资源库
  - 报表导出
  - 公告和更多培训衍生对象
- 增强运维能力：
  - 角色数据范围说明
  - 角色数据范围变更审计
  - 数据范围命中结果日志

**优先级**

- `P0，已完成首轮落地，后续继续扩面`

### 4.6.2 OA 审批结果承接

**需求目标**

- 平台不做 OA 审批，但要承接审批完成结果

**当前状态**

- 未看到外部 OA 回调或审批结果同步能力

**差距说明**

- 如果不补这一层，开班发布、资料上架等场景只能手工代替

**建议实现内容**

- 提供两种模式：
  - 手工登记外部审批结果
  - 外部系统回调接口
- 所有外部审批都应留痕：
  - 审批单号
  - 审批结果
  - 同步时间
  - 同步人/来源

**优先级**

- `P1，可在一期内按最小闭环补齐`

### 4.6.3 留痕与审计

**需求目标**

- 关键业务动作必须可追溯

**当前状态**

- 当前模型中有部分创建时间、更新时间
- 但缺少统一的业务操作日志

**差距说明**

- 发布、锁定、解锁、补签、评课修正、审核、上架等关键动作都需要留痕

**建议实现内容**

- 新增统一操作日志体系
- 日志至少记录：
  - 操作人
  - 操作对象
  - 变更前后值
  - 操作时间
  - 来源端

**优先级**

- `P0，一期必须补齐`

### 4.6.4 导出能力统一

**需求目标**

- 培训记录、授课记录、年度考核等都要支持导出

**当前状态**

- 当前已有看板报表导出
- 但业务导出仍不系统

**差距说明**

- 现有导出偏展示层，不是业务台账层

**建议实现内容**

- 统一导出中心：
  - 培训名单导出
  - 签到签退导出
  - 学员训历导出
  - 教官授课记录导出
  - 评课统计导出

**优先级**

- `P0，一期必须补齐`

### 4.6.5 通知与提醒

**需求目标**

- 对报名结果、签到异常、审核结果、开班通知等进行提醒

**当前状态**

- 当前没有系统化的站内通知/消息中心

**建议实现内容**

- 新增：
  - 站内通知
  - 待办提醒
  - 短信/微信等外部通知预留接口

**优先级**

- `P1，一期可按最小版本接入`

### 4.6.6 智能体中枢与二期 AI 接入

**需求目标**

- 为二期 AI 评分等能力预留智能体中枢接入点

**当前状态**

- 当前已有单点 AI 接口，但不是统一的智能体接入架构

**建议实现内容**

- 二期再做统一 AI 能力接入层：
  - 评分智能体
  - 课程策划智能体
  - 资源推荐智能体

**优先级**

- `P2，二期规划`

## 5. 建议实施顺序

### 5.1 P0：一期必须补齐

- 班次发布、锁定、分组、班干部
- 培训基地运营增强、基地维度统计与导出
- 学员签退、评课、缺勤闭环、训历档案、年度导出
- 教官在线采集
- 考试场次重构、考试与班次绑定、准入制考试
- 视频公共资源库运营、移动端短视频浏览、征集与分级审核
- 角色重构、数据范围扩面、留痕体系、导出中心

### 5.2 P1：一期增强项

- 外部 OA 结果承接
- 按警种隔离题库
- 通知中心
- 教官评聘报表
- 学员移动端自服务完善

### 5.3 P2：二期规划

- 师资库/课程库/购物车式选课
- AI 主观题评分
- 等级认证考试
- 视频与培训联动
- 统一智能体中枢

## 6. 结论

如果按需求报告来衡量，当前项目已经不只是“平台骨架”，而是完成了考试、培训和数据范围控制的一期首轮重构。但离“一期完整上线版本”仍有明显距离，接下来应继续优先补齐以下三类闭环：

- 培训班运营闭环：基地维度统计、解锁审批、完整审计台账
- 学员 / 教官行为闭环：年度训历导出、移动端自服务
- 培训资源与考试闭环：按警种题库深度运营、视频征集与移动端体验、认证类考试

换句话说，一期最重的“考试分层 + 培训流程化 + 学员过程闭环 + 首轮数据域控制”已经进入可用状态，接下来应从“能跑”推进到“可运营、可统计、可审计”。 
