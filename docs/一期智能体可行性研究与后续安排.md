# 一期智能体可行性研究与后续安排

## 1. 文档说明

本文档基于以下信息整理：

- 你提供的一期智能体目标与边界
- 仓库级说明 `README.md`
- 后端说明 `backend/README.md`
- 当前代码基线中的 AI 任务流、培训排课、训历、推荐与前端路由实现

本文档目标不是再写一版需求，而是回答四个问题：

1. 当前平台是否适合在一期落地“建议型智能体”
2. 哪些能力可以直接复用，哪些能力必须新增
3. 一期应如何切分范围，才能快速上线并便于汇报
4. 后续实施应如何排期、分工和验收

评估时间基线：`2026-03-19`

## 2. 结论摘要

## 2.1 总体结论

一期做成“建议型智能体”是可行的，而且是当前平台最稳妥、最容易落地的方案。

原因有三点：

- 平台已经具备统一 AI 任务流、培训排课存储、课程变更留痕、训历归档、资源推荐等基础设施，不需要从零搭中台
- 培训管理已经形成“人工编辑 -> 应用 -> 留痕”的业务习惯，天然适合“AI 给建议、人工确认后生效”
- 现有 AI 能力已经走的是任务化、确认式流程，一期延续相同产品范式，用户学习成本和研发成本都最低

## 2.2 推荐落地顺序

推荐按以下顺序推进：

1. 先落地 `智慧排课智能体`
2. 再落地 `个性化训练智能体`

原因：

- 排课场景价值最直接，汇报效果最好，数据口径也最明确
- 当前培训域已有 `TrainingCourse.schedules`、课次状态流和变更日志，排课建议更容易形成闭环
- 个性化训练虽然也具备基础数据，但“画像口径”和“方案模板”仍需补一层聚合逻辑，复杂度高于排课

## 2.3 可行性评级

| 能力 | 可行性 | 判断 |
| --- | --- | --- |
| 智慧排课智能体 | 高 | 可直接复用现有培训课程、课次、流程、权限和变更留痕体系 |
| 个性化训练智能体 | 中高 | 数据基础已具备，但需要新增画像聚合、方案模板和方案快照 |
| 无人值守自动执行 | 低 | 当前不建议，一期应坚持人工确认后生效 |

## 3. 当前系统基础映射

## 3.1 已具备的后端基础

### 3.1.1 统一 AI 任务流已存在

当前系统已经有统一 AI 任务模型和任务型接口：

- `backend/app/models/ai_task.py:11` 已有 `AITask`
- `backend/app/models/ai_task.py:18` 已有 `task_type`
- `backend/app/models/ai_task.py:20`、`backend/app/models/ai_task.py:21` 已有 `request_payload`、`result_payload`
- `backend/app/views/ai.py:37` 到 `backend/app/views/ai.py:270` 已形成“创建任务、查看任务、更新结果、确认结果”的接口模式
- `backend/app/tasks/ai_question.py:81` 已有基于 Celery 的异步任务调度方式

这说明“一期新增两个任务类型”不是新建体系，而是在既有任务流上扩展。

### 3.1.2 培训排课数据结构已存在

培训排课相关基础已经具备：

- `backend/app/models/training.py:92` 已有 `TrainingCourse`
- `backend/app/models/training.py:107` 已有 `schedules` JSON，可承载课次明细、状态和签到签退生命周期
- `backend/app/services/training.py` 已实现课程更新、课次状态刷新、当前课次识别、签到签退流转
- `backend/app/models/training.py:235` 已有 `TrainingCourseChangeLog`
- `backend/app/services/training_course_change.py` 已实现课程/课次 diff 和记日志能力

这意味着排课建议确认后，可以直接回写现有培训课程结构，不需要再新建一套排课表。

### 3.1.3 学员训练数据已具备聚合基础

个性化训练依赖的数据也已存在：

- `TrainingHistory`：训历归档
- `Enrollment`：参训关系
- `CheckinRecord`：出勤与评课
- `CourseProgress`：课程学习进度
- `ExamRecord`：考试成绩
- `ResourceBehaviorEvent`、`ResourceRecommendScore`：资源行为和推荐分

其中推荐能力已有基础实现：

- `backend/app/services/recommendation.py:19` 已支持行为记录
- `backend/app/services/recommendation.py:53` 到 `backend/app/services/recommendation.py:60` 已支持预计算推荐分读取
- `backend/app/services/recommendation.py:159` 已支持基于行为推导兴趣权重

这意味着个性化训练方案不需要等待“新画像系统”建设完成，可以先做“聚合画像快照”。

### 3.1.4 培训前端入口已具备

前端已有可挂载的业务入口：

- `frontend/src/router/index.js:135` 到 `frontend/src/router/index.js:150` 已有周训练计划页、培训详情页
- `frontend/src/views/training/Schedule.vue` 已具备当前排课编辑和导入能力
- `frontend/src/api/ai.js:4` 到 `frontend/src/api/ai.js:60` 已有统一 AI 任务接口封装模式

这意味着一期前端不需要新造导航体系，直接在培训详情页、周训练计划页和 AI 任务页扩展即可。

## 3.2 当前系统的结构性缺口

虽然可行，但当前代码基线也有几个必须正视的缺口。

### 3.2.1 AI 任务模型目前偏考试域

`AITask` 当前虽然通用，但确认结果字段仍明显偏向考试域：

- `confirmed_question_ids`
- `confirmed_paper_id`

这套字段适合“出题/组卷”，不适合“排课建议/个训方案”。如果继续往 `AITask` 上堆领域字段，后续会越来越耦合。

建议：

- `AITask` 保留为统一任务主表
- 新增领域侧扩展表或通用确认快照字段，不要把排课/个训结果硬塞进现有试卷确认字段

### 3.2.2 排课约束数据还不够结构化

当前系统已有课程、教官和课次，但还没有现成的“教官不可用时间、场地不可用时间、固定课次锁定”等独立主数据模型。

这不影响一期落地，但会影响排课效果上限。

建议一期做法：

- 先把这些约束作为任务表单里的 JSON 输入
- 先满足“人工临时约束录入”
- 二期再升级为长期可复用的基础台账

### 3.2.3 个性化训练还没有“方案快照”对象

当前存在训练数据和推荐数据，但没有“某学员在某时点生成并确认的训练方案”实体。

这会带来两个问题：

- 无法做版本留存
- 无法支持“本人查看”和“教官查看学员”的稳定历史查询

因此一期至少应新增一张个训方案快照表。

## 4. 一期建设原则

## 4.1 产品原则

- AI 只给建议，不直接无人值守写库
- 规则引擎负责硬约束，模型负责解释、补充、生成备选方案
- 所有应用动作必须人工确认，并保留留痕
- 尽量复用现有培训、推荐、AI 任务和权限体系

## 4.2 技术原则

- 以 `AITask` 为统一任务入口，不再单独造一套“智能体任务中心”
- 规则先行，模型后置，避免模型直接决定关键业务状态
- 结果落库分两类：
  - 排课：确认后回写 `TrainingCourse.schedules`，并写 `TrainingCourseChangeLog`
  - 个训：确认后保存方案快照，不直接改业务核心数据
- 任务结果要可预览、可编辑、可二次确认

## 4.3 一期边界

一期做：

- 排课建议
- 调课建议
- 冲突检测
- 个训方案
- 资源推荐
- 方案说明与备选方案

一期不做：

- 无人值守自动执行
- OA 审批联动
- 全量师资排班中台
- 复杂外部系统同步
- 通用智能体平台重构

## 5. 一期目标设计

## 5.1 智慧排课智能体

### 5.1.1 建设目标

解决四类问题：

- 排课慢
- 冲突多
- 调课难
- 周计划补排依赖人工经验

### 5.1.2 目标用户

- 培训管理员
- 班主任
- 教官

### 5.1.3 核心场景

- 新班初排
- 周计划补排
- 临时调课
- 冲突扫描

### 5.1.4 一期建议能力

- 创建 AI 排课任务
- 自动生成 `1 个主方案 + 2 个备选方案`
- 输出冲突清单、调课建议、原因说明
- 支持人工微调后确认应用
- 应用后保留现有课程变更日志

## 5.2 个性化训练智能体

### 5.2.1 建设目标

解决三类问题：

- 训练内容一刀切
- 学员短板识别不准
- 资源推荐和训练计划未形成统一闭环

### 5.2.2 目标用户

- 学员本人
- 教官
- 班主任

### 5.2.3 一期建议能力

- 自动识别个人短板标签
- 生成阶段性训练方案
- 推荐匹配课程/资源/练习项
- 给出训练目标、周期和建议频次
- 方案确认后保存快照，支持查看历史版本

## 6. 推荐总体架构

## 6.1 架构分层

建议采用四层架构：

1. 数据聚合层
2. 规则计算层
3. 大模型生成层
4. 人工确认与落库层

## 6.2 数据输入

### 6.2.1 排课智能体输入

- `Training`
- `TrainingCourse`
- `Enrollment`
- `CheckinRecord`
- 培训考试安排
- 教官信息
- 培训基地和地点信息
- 人工补充约束 JSON

### 6.2.2 个训智能体输入

- `TrainingHistory`
- `CourseProgress`
- `ExamRecord`
- `CheckinRecord`
- `ResourceBehaviorEvent`
- `ResourceRecommendScore`
- 用户基础信息
- 当前培训班或当前训练阶段信息

## 6.3 处理链路

### 6.3.1 排课智能体链路

1. 聚合培训班、课程、课次、考试、教官和约束输入
2. 执行硬约束校验
3. 执行软约束优化
4. 产出结构化主方案与备选方案
5. 由大模型生成说明文案、冲突原因和调课建议
6. 人工确认后回写课次

### 6.3.2 个训智能体链路

1. 聚合训历、考试、学习进度、资源行为
2. 计算短板标签、能力等级和推荐方向
3. 生成结构化训练方案
4. 由大模型生成自然语言说明和执行建议
5. 人工确认后保存方案快照

## 7. 详细可行性分析

## 7.1 智慧排课智能体可行性

### 7.1.1 为什么可行

当前平台已经具备排课闭环所需的大部分基础：

- 排课承载结构已存在，不需要新建课表系统
- 课程更新接口和导入能力已存在，可复用应用通道
- 课程变更留痕已存在，可满足“AI 应用后留痕”
- 周训练计划页已存在，可直接挂 AI 建议入口

### 7.1.2 一期最小实现建议

排课任务表单建议包含：

- `training_id`
- 排课范围
- 排课目标
- 单日最大课时
- 理论/实操比例
- 是否避开考试日
- 固定课次
- 教官不可用时间
- 场地不可用时间

规则引擎至少覆盖以下硬约束：

- 时间不超出培训周期
- 已开始/已结束课次不可改
- 同一教官不可撞时间
- 同一课次不可重叠
- 考试时段不可冲突

软约束至少覆盖：

- 理论/实操更均衡
- 重点课程适当前置
- 单教官连续授课不过载
- 地点尽量集中

### 7.1.3 一期落库建议

建议采用“任务结果快照 + 确认应用”的方式：

- 任务生成阶段：结果放在 `AITask.result_payload`
- 人工确认阶段：调用专门确认接口
- 应用阶段：回写 `TrainingCourse.schedules`
- 留痕阶段：复用 `TrainingCourseChangeLog`

### 7.1.4 需要新增的后端能力

- 新任务类型：`schedule_generation`
- 新服务：`ScheduleAgentService`
- 新接口：
  - `POST /ai/schedule-tasks`
  - `GET /ai/schedule-tasks`
  - `GET /ai/schedule-tasks/{id}`
  - `PUT /ai/schedule-tasks/{id}/result`
  - `POST /ai/schedule-tasks/{id}/confirm`

### 7.1.5 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 教官/场地约束不完整 | 排课质量波动 | 一期先人工补充约束，逐步沉淀台账 |
| 规则不全导致建议不可用 | 应用率低 | 先把硬约束做全，软约束分层上线 |
| 用户担心 AI 乱改课表 | 使用阻力大 | 坚持预览、微调、确认后生效 |

## 7.2 个性化训练智能体可行性

### 7.2.1 为什么可行

当前平台虽然没有显式“学员画像表”，但已经具备构建一期画像快照所需的数据：

- 训历可反映参训经历和出勤表现
- 学习进度可反映课程完成情况
- 考试记录可反映知识掌握情况
- 资源行为与推荐分可反映兴趣偏好

因此一期无需等待完整画像中台，可以直接做“任务时聚合画像”。

### 7.2.2 一期最小实现建议

建议先支持以下方案结构：

- 训练目标
- 当前短板标签
- 周训练计划
- 推荐课程/资源
- 建议练习频次
- 预计提升周期
- 教官建议话术

建议先支持两类触发方式：

- 针对单个学员生成
- 针对某培训班批量生成

### 7.2.3 一期落库建议

个训方案不直接改业务数据，建议新增快照表，例如：

- `personal_training_plan_snapshots`

关键字段建议包括：

- `task_id`
- `training_id`
- `user_id`
- `portrait_snapshot`
- `plan_snapshot`
- `status`
- `confirmed_by`
- `confirmed_at`
- `version_no`

### 7.2.4 需要新增的后端能力

- 新任务类型：`personal_training_plan_generation`
- 新服务：`PersonalTrainingPlanAgentService`
- 新接口：
  - `POST /ai/personal-training-tasks`
  - `GET /ai/personal-training-tasks`
  - `GET /ai/personal-training-tasks/{id}`
  - `PUT /ai/personal-training-tasks/{id}/result`
  - `POST /ai/personal-training-tasks/{id}/confirm`

### 7.2.5 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 画像口径不统一 | 方案可信度不足 | 一期先定义固定标签口径，不做过度个性化 |
| 推荐结果解释性差 | 教官不愿采用 | 输出短板依据、推荐依据和资源来源 |
| 批量生成成本较高 | 异步任务积压 | 复用现有 Celery 任务流，采用异步生成 |

## 8. 推荐数据与模型改造

## 8.1 推荐的数据改造

建议最小新增以下数据对象：

- `AI schedule` 领域扩展表，记录 `training_id`、范围、目标和约束
- `personal_training_plan_snapshots`，用于方案确认后的历史沉淀

如果想保持一期更轻量，也可以先不建独立 `AI schedule` 表，而是把任务参数放在 `AITask.request_payload` 中；但长期看，查询、统计和按培训班聚合会比较差，不推荐作为最终结构。

## 8.2 推荐的服务拆分

建议新增以下服务，而不是把逻辑直接堆进现有 `AIService`：

- `ScheduleAgentService`
- `ScheduleRuleEngine`
- `SchedulePromptBuilder`
- `PersonalTrainingPlanAgentService`
- `TrainingPortraitAggregator`
- `TrainingPlanPromptBuilder`

这样可以避免继续放大现有 `AIService` 的任务类型分支和重复代码。

## 8.3 权限建议

### 8.3.1 排课智能体

- 仅培训管理员、班主任可创建和确认
- 教官可查看建议结果，但默认不直接确认应用

### 8.3.2 个训智能体

- 学员可查看自己的确认方案
- 教官和班主任可查看所管理学员的方案
- 管理员可查看全局

## 9. 前端落点建议

## 9.1 智慧排课智能体

- 培训详情页新增 `AI排课`
- 周训练计划页新增 `AI建议排课`、`冲突检测`、`应用方案`
- AI 任务页新增 `排课任务列表/详情`

## 9.2 个性化训练智能体

- 培训详情页学员名单中新增 `生成个训方案`
- 学员个人训历页新增 `AI个训方案`
- AI 任务页新增 `个训任务列表/详情`

## 9.3 前端实现建议

优先复用现有 AI 任务页交互模式：

- 表单创建任务
- 列表查看状态
- 详情页编辑结果
- 确认后生效

这样可以减少新交互设计成本，也更便于汇报时展示“平台已有 AI 任务体系持续扩展”的连续性。

## 10. 一期验收口径

## 10.1 智慧排课智能体

- 能自动生成可预览课表
- 能识别并提示硬冲突
- 能生成主方案和备选方案
- 能支持人工微调后应用
- 应用后在现有排课页、签到签退流程中可正常使用
- 应用后能在课程变更记录中看到留痕

## 10.2 个性化训练智能体

- 能基于现有训练数据生成个人训练方案
- 能展示短板识别依据和推荐依据
- 能推荐课程/资源
- 能人工确认并保存方案快照
- 学员本人和教官都能按权限查看

## 11. 后续实施安排

## 11.1 建议分阶段推进

### 第一阶段：方案冻结与数据摸底，1 周

目标：

- 冻结一期范围
- 确认排课约束字段
- 确认个训标签口径和方案模板
- 盘点教官、课程、考试、资源数据质量

输出物：

- 任务字段清单
- 约束规则清单
- 个训标签与模板清单
- 演示脚本草案

### 第二阶段：智慧排课智能体开发，2 周

目标：

- 完成排课任务接口、规则引擎、结果结构和确认应用
- 接入培训详情页与周训练计划页
- 打通变更留痕

输出物：

- 可创建和查看的排课任务
- 主方案、备选方案、冲突清单、方案说明
- 一键应用并回写课表

### 第三阶段：个性化训练智能体开发，2 周

目标：

- 完成画像聚合、个训方案生成、方案快照确认
- 接入学员端和教官端查看入口

输出物：

- 个人训练方案
- 推荐资源清单
- 方案快照和历史版本

### 第四阶段：联调、试点与汇报材料，1 周

目标：

- 做真实数据联调
- 完成权限校验和异常场景验证
- 形成演示环境和汇报 PPT 要点

输出物：

- 演示账号和演示路径
- 验收清单
- 汇报口径和截图素材

## 11.2 建议排期

| 周次 | 重点事项 | 主要输出 |
| --- | --- | --- |
| 第 1 周 | 需求冻结、数据摸底、字段清单 | 详细设计和字段口径 |
| 第 2 周 | 排课任务后端与规则引擎 | 排课任务接口和结果结构 |
| 第 3 周 | 排课前端与应用确认 | 可演示的 AI 排课闭环 |
| 第 4 周 | 个训画像聚合与方案生成 | 个训任务接口和快照结构 |
| 第 5 周 | 个训前端、权限和资源推荐联动 | 可演示的个训方案闭环 |
| 第 6 周 | 联调、试点、验收和汇报准备 | 试点版本和汇报材料 |

## 11.3 建议人员安排

最小可行团队建议如下：

- 产品/需求 1 人：负责场景收敛、表单字段和验收口径
- 后端 2 人：一人主攻排课，一人主攻个训与任务流扩展
- 前端 1 人：负责培训详情页、周计划页、AI 任务页改造
- AI/算法 1 人：负责规则编排、提示词和结果结构设计
- 测试/实施 1 人：负责场景验证、试点支持和汇报联调

如果资源有限，`AI/算法` 也可由后端兼任，但前提是一期坚持“规则优先、模型补充说明”的简化路线。

## 11.4 汇报建议

一期汇报建议突出四个点：

- 不是空中楼阁，而是复用现有平台快速扩展
- 不是让 AI 直接替人决策，而是“建议 + 人工确认”
- 不是只给文案，而是能真正落到现有排课和个训业务
- 不是黑盒，而是保留规则校验、解释说明和应用留痕

建议演示链路：

1. 创建 AI 排课任务
2. 查看主方案、备选方案和冲突清单
3. 人工微调并确认应用
4. 回到周训练计划页查看已生效课表
5. 查看课程变更记录中的 AI 应用留痕
6. 为某学员生成个训方案
7. 展示短板标签、推荐资源和确认后的方案快照

## 12. 最终建议

从当前仓库基线看，一期智能体最合理的落地方式不是新建“大而全智能体平台”，而是：

- 以现有 `AITask` 为任务骨架
- 以现有培训域和推荐域数据为输入
- 以规则引擎做安全兜底
- 以大模型做说明、补充建议和文案生成
- 以人工确认作为唯一生效入口

按这个路径推进，一期完全有机会在 `6 周左右` 做出可演示、可试点、可汇报的版本。

其中最推荐的切入顺序是：

1. 先把 `智慧排课智能体` 做成完整闭环
2. 再补 `个性化训练智能体`
3. 试点稳定后，再考虑自动化程度和外部系统联动
