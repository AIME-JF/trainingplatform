# 智慧教育训练平台 - 缺陷测试反馈报告 (DeBug README)

该文档总结了在“培训管理”及相关功能手动测试过程中发现的各项缺陷与异常。主要涉及状态同步、数据流转持久化、页面交互跳转、UI逻辑排版以及仪表盘真实数据统计口径等问题。

---

## 🐞 一、 培训班管理列表及详情缺陷

1. **培训班状态与日期不同步**
   - **问题描述**：新建培训班并设定培训日期区间后，即使“当前系统日期”已进入课程的开启区间，该班级在列表和详情中的状态标签依然显示为“未开始”。
   - **期望效果**：系统应当根据当前时间动态判定状态，变为“进行中”。
2. **进度条标签计算不合理**
   - **问题描述**：培训班进度条状态非0即100（没开始是0，结束了是100），没有根据真实的时间流逝或学员任务完成情况进行同步运转。
3. **主责教官信息渲染错误**
   - **问题描述**：新建培训班时已人工选定特定的“主管教官”关联该培训班。但在管理列表的班级标签和详情页的“主管/班主任”处，均被错误覆盖或渲染为“系统管理员”。
   - **期望效果**：需正确展示在创建阶段绑定的真实教官姓名。
4. **课程与学员配置无持久化**
   - **问题描述**：在培训班详情页下部添加“课程”及“学员”名单后操作无法有效保存。退出详情页再重新进入，所有刚挂载的课程与学员数据全部清空丢失。
5. **学员详情页路由跳转失效**
   - **问题描述**：在加载出来的培训班学员名单中，点击“学员名字”无法跳转。
   - **期望效果**：点击超链接名称应触发路由，跳转穿透至“学员库”内的对应学员详细档案页。

---

## 📋 二、 签到打卡与数据对齐问题

1. **签到动作无动态反馈**
   - **问题描述**：在“开班签到页面”完成签到后，页面没有立刻给出即时的【签到完成率】变更提示或气泡反馈。
2. **详情页签到统计信息不真实且逻辑割裂**
   - **问题描述**：培训班详情内的两个签到率仪表展示（开班签到占比、课程总签到率圆环）为固定虚假数据，未能与真实打卡动作联动。
   - **校正规则**：
     - **开班签到率**：必须严格与“开班签到页”统计数据聚合对齐。
     - **课程总签到率**：需抓取“课程签到页”内所有设定了“已开启签到”课程的具体打卡完成情况百分比，并将这些数据求均值后展示。

---

## 📅 三、 周训练计划（日程表）交互展示异常

1. **由培训班至日程表的关联跳转失效**
   - **问题描述**：在培训班详情页内点击“查看日程”，未能正常跳转至整体日程表内对应的班级视图。
2. **班级内课程数据多端未打通**
   - **问题描述**：当手动切换至周训练计划中并选中某一特定班级时，展列出的“课程计划排表”完全与前面详情页内绑定的内部课程不一致（数据隔离）。
3. **排表周期时间轴展示畸形**
   - **问题描述**：日程表表头的周周期区间错误地从“周五跨度到周二”。
   - **期望效果**：应变更为标准的“周一至周日”七天制排版。
4. **课程缺少动态交互（拖拽排版功能缺失）**
   - **问题描述**：安排表内的静态课程模块被锁定，无法通过鼠标拖动来动态调整所占据的时间段或调换课程顺序。

---

## 📊 四、 培训数据看板指标统计及接口调用异常

当前看板内指标严重依赖前端写实的 Mock 数据，同时图表口径失准。后续需对接后端真实接口，并修改统计聚合口径要求如下：

1. **“进行中的班级”指标**
   - **口径**：应该统计此时此刻处于（`当前时间 >= 开始日期 并且 当前时间 <= 结束日期`）区间内的班级真实数量。
2. **“本月参训人数”指标**
   - **口径**：精准统计本月内参加过课程培训的去重学员总数（需涵盖在本月期间的“进行中”记录以及“已结束”记录的所有学员库关联）。
3. **“本月培训完成率”指标**
   - **口径**：需基于“当月完成培训”这批限定特征的学员样本，提取他们所有已结束课程最终通过的完成率平均值作为计算结果。
4. **“待审核学员”指标**
   - **口径**：应聚合全平台中针对所有课程业务链路上的“待人工审核通过进入班级申请”状态学员的工单总数。
5. **“各市局本月参训人数统计图”**
   - **口径**：调取“本月实际参训”的所有学员列表，通过关联对应的地市公安局属地属性来进行横向聚合归类与柱状展现。
6. **“近6月完成培训趋势折线图”**
   - **口径**：应展示过去相邻 6 个月维度中，所有“已结束”状态的培训班学员完成率走值的均值线（非写死常数）。
7. **“各地市完成率排名”**
   - **口径**：调取各市局所有下属参选学员的历史培训考核完成率，平均取值后进行的排行榜单呈现。
8. **“生成报告”按钮未能联动后台**
   - **问题描述**：位于看板页面内的生成报告下载触点未能生成真实的 Word/Excel 以及 PDF 等载体。
   - **期望效果**：按下后需请求后端导出网关，抓取真实统表数据返回数据流后下载至本地。
